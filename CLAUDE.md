<!-- SPECKIT START -->
This working directory is **araratsec-agent** (Repo B, the audit CapabilityPack,
package `audit_agent`), which runs on **secure-agent-kernel** (Repo A, package
`sr_agent`) as a pinned dependency — see specs/048-repo-split/. Dev uses an
editable sibling checkout of the kernel at ../secure-agent-kernel.

For prior architecture context, read: specs/043-compiled-attempt-checkpoint/plan.md
<!-- SPECKIT END -->
